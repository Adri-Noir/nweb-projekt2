import React from "react";
import Header from "@/components/Header";
import SDE from "@/components/SDE";

const sde = () => {
  return (
    <Header>
      <SDE />
    </Header>
  );
};

export default sde;
